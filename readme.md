# Project Overview

This project contains a scheduling model and a small experimental pipeline for evaluating different production configurations.

The core optimization model is implemented in `experiment_pipeline.py`.
The experiment workflow, including the execution of different experiment settings, is implemented in `run_experiment.py`.
The creation of diagrams and boxplots for result analysis is implemented in `diagram_generation_boxplot.py`.
